{{ if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: kube-system
  name: kom-operator
  labels:
    provider: kaiso
    version: "{{.Chart.AppVersion }}"
{{ end }}